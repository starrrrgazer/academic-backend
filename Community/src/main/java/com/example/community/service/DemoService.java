package com.example.community.service;
//服务层，用于实现后端的逻辑（写调用的方法），并且这里要调用数据层dao

import com.example.community.entity.demo;
import org.springframework.stereotype.Service;

@Service
public class DemoService {
    //这里定义接口函数
}
